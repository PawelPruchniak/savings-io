spring:
  liquibase:
    change-log: db/liquibase.changelog-master-it.xml
    parameters:
      index-tablespace: pg_default
      data-tablespace: pg_default

logging:
  level:
    pp.pl.io.savings: DEBUG
